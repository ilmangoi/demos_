// !插件模板，插件应以以下格式编写，否则自动引入插件时可能会出错

export default {
  // eslint-disable-next-line
  install(_Vue) {
    //全局过滤器
    // _Vue.filter('mySlice', function (value) {
    //   return value.slice(0, 4)
    // })
    //全局指令
    // .....
  }
}
